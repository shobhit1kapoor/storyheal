#!/bin/bash
# Reset admin password script for Docker container
# Usage: docker exec storyheal-api resetadmin

set -e

# Change to app directory
cd /app

# Run the Python reset script
python scripts/reset_admin_password.py

